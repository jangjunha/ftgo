spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    show-sql: ${SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: ${SHOW_SQL:false}
eventuate:
  outbox:
    id: 8105
grpc:
  server:
    port: 8105
